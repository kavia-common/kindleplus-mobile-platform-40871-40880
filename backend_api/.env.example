# Backend API configuration (.env)
# Copy this file to ".env" in the backend_api folder and adjust values for your environment.

# Application
PROJECT_NAME="KindlePlus Backend API"
PROJECT_DESCRIPTION="Backend API for the KindlePlus mobile platform."
ENVIRONMENT="development"
APP_VERSION="0.1.0"

# Security
# IMPORTANT: Change this in production
SECRET_KEY="please-change-me"

# CORS
# Comma-separated list of allowed origins. Use "*" to allow all (development only).
# Example: "http://localhost:3000,https://admin.kindleplus.app"
CORS_ORIGINS="*"

# Database
# Prefer a robust DB in production, e.g. Postgres (requires appropriate driver).
# Example: postgresql+psycopg://user:password@localhost:5432/kindleplus
# Default (if not set): SQLite at backend_api/data/app.db
# DATABASE_URL="postgresql+psycopg://user:password@localhost:5432/kindleplus"

# Docs URLs (optional overrides)
# DOCS_URL="/docs"
# REDOC_URL="/redoc"
# OPENAPI_URL="/openapi.json"
